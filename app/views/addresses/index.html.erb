<h1><%= @person.name%>'s Address is:</h1>

<% if @address.blank? %>
<h3> No address. Please add an address </h3>
  <%= link_to 'Add Address', new_person_address_path(@person) %>
<% else %>
   <h3> <%= @address.street_address %> </h3>
   <h3> <%= @address.city + ", " + @address.state %>
   <h3> <%= @address.zip %> </h3>
<% end %>
